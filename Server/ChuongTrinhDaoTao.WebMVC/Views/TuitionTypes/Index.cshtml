@model IEnumerable<ChuongTrinhDaoTao.WebMVC.Models.TuitionTypeDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="col-lg-12 grid-margin bg-white stretch-card">
    <div class="card">
        <div class="card-body">
            <a class="btn btn-outline-success btn-sm" style="width:200px;margin-bottom:20px;" asp-controller="TuitionTypes" asp-action="Create">Create</a>

            <div class="table-responsive">
                <table id="bootstrap-data-table" class="table">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.TuitionTypeId)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.TuitionTypename)
                            </th>
                           
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TuitionTypeId)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TuitionTypename)
                                </td>
                                <td>
                                    <a asp-action="Details" asp-controller="TuitionTypes" asp-route-id="@item.TuitionTypeId" class="btn btn-info btn-icon-text">Detail</a>
                                    <a asp-action="Edit" asp-controller="TuitionTypes" asp-route-id="@item.TuitionTypeId" class="btn btn-success btn-icon-text">Edit</a>
                                    <a asp-action="Delete" asp-controller="TuitionTypes" asp-route-id="@item.TuitionTypeId" class="btn btn-danger btn-icon-text">Delete</a>
                                </td>
                            </tr>
                        }


                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- Scripts -->

